
{
  "name": "copytrade-platform",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step required for this ESM-based app' && exit 0"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  }
}
